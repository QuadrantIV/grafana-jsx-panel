version: '3.0'

services:
  grafana:
    container_name: 'grafana-jsx-panel'
    platform: 'linux/amd64'
    build:
      context: ./.config
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana}
        grafana_version: ${GRAFANA_VERSION:-latest}
    ports:
      - 3000:3000/tcp
    volumes:
      - ./dist:/var/lib/grafana/plugins/grafana-jsx-panel
      - ./provisioning:/etc/grafana/provisioning
